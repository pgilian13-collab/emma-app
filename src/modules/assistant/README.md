# Módulo · Asistencia para Dibujar

Calca dibujos usando la cámara del dispositivo.

**Estado:** pendiente de implementación.

Estructura prevista:

```
assistant/
├── components/   # CameraView, OverlayCanvas, ControlsPanel
├── hooks/        # useCamera, useOverlayTransform
├── services/     # cameraService, overlayStorage
├── types/        # OverlayTransform, CameraDevice
└── pages/        # AssistantPage (cuando crezca)
```
