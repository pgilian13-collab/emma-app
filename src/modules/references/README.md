# Módulo · Referencias

Biblioteca visual categorizada.

**Estado:** pendiente de implementación.

Estructura prevista:

```
references/
├── components/   # ReferenceGrid, ReferenceCard, CategoryFilter, ReferenceEditor
├── hooks/        # useReferences, useSearch
├── services/     # referenceStorage (IndexedDB), categoryService
├── types/        # Reference, ReferenceCategory
└── store/        # referencesStore
```