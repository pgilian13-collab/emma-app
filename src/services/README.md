# Servicios compartidos

Capa de servicios para acceso a almacenamiento, sistema de archivos, etc.

**Estado:** pendiente de implementación.

Servicios previstos:

- `storageService` — abstracción de LocalStorage / IndexedDB
- `mediaService` — utilidades para metadatos de audio e imagen
- `clipboardService` — copiar al portapapeles
```